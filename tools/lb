#!/usr/bin/env bash

# Loqa Backlog - Convenient wrapper for backlog-aggregator.sh
# Usage: lb [command] [options]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/backlog-aggregator.sh" "$@"